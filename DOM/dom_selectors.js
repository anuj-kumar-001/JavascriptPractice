// document.getElementById('title')
// document.querySelector('h1')
// document.querySeclector(#headings / .className)


// html creates the page -> browser converts html into the dom -> javascript uses the dom to manipulate the page
// so dom manipulation simply means using javascript to change the webpage dynamically

// using dom we can do thing like
// document.getElementById('title').innerText="hello anuj"   // change text
// document.getElementById('title').style.color="red";       // changes color
// document.getElementByClass('heading').style.display="none"    // hides something
// document.getelementById('title').src="newphoto.jpg";         // change an image
// document.getElementById('title').addEventListener("click" , function() { console.log("button clicled !")});


// we have three common selectors 
 
// innerHTML-- returns the html markup inside the element
// innerText-- returns the visible text of an element
// textContent--return all text content, including text inside hidden elements